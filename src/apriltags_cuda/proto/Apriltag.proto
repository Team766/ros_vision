syntax = "proto3";

package apriltags_cuda;

message ApriltagProto {
    double collect_time = 1;
    int32 tag_id = 2;
    double x = 3;
    double y = 4;
    double z = 5;
    // TODO: consider adding covariance, rotation
}

message ApriltagListProto {
    repeated ApriltagProto tags = 1;
}
